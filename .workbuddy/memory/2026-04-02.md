# 2026-04-02 工作日志

## Home.vue 改为自适应布局
- 移除了 `STAGE_W=1920` / `STAGE_H=960` 固定画布常量
- 删除了 `updateStage()` 函数及 `resize` 事件监听
- 删除了 `shift` / `scaleRef` / `posRef` ref 变量
- `stageStyle` computed 改为 `width: 100%, height: 100%, position: relative`
- `.page-bg` CSS 从 `1920px/960px` 改为 `100%/100%`
- `.stage` CSS 补充 `width: 100%; height: 100%`
- 整体布局现在随浏览器窗口自适应，不再使用 scale 缩放方案
