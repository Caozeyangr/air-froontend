# 2026-04-16 工作日志

## 影像底图替换
- 将 `CesiumViewer.vue` 中地图底图从本地单张图片（`SingleTileImageryProvider` + `背景.png`）改为 XYZ 瓦片服务（`UrlTemplateImageryProvider`）
- 瓦片地址：`http://agri.cangling.cn:22002/api/v1/map3/ce0a07f5ef1062b8c90047d2051efb0c742bb8795bc9be5e24b1bbf7016f781a/{z}/{x}/{y}.png`
- 同步更新了 `updateBaseImageryRectangle` 函数，去掉了 `bgImg` 的 import

## 点位聚合功能
- 在 `CesiumViewer.vue` 中新增点位聚合逻辑
- 阈值：`CLUSTER_ZOOM_THRESHOLD = 800000`（相机高度，单位：米）
- 聚合半径：`CLUSTER_PIXEL_RADIUS = 60`（屏幕像素）
- 远视图（相机高度 > 800km）显示聚合点，近视图显示具体点位
- 聚合点使用蓝色圆形图标，显示聚合数量
- 监听 `camera.changed` 事件动态更新聚合状态

## Workstation.vue 工作站监控面板
- 在 `src/view/Workstation.vue` 中实现工作站资源监控面板
- 包含模块：
  - 当前时间（实时更新）
  - 资源申请记录（5项统计指标）
  - 物理机状态（3项状态指标）
  - 设备GPU使用量（ECharts 柱状图）
  - 资源分配统计（4个仪表盘：CPU/内存/存储/GPU）
  - 资源使用率监测（4个趋势图）
  - 设备GPU使用量表格（2个 Element Plus 表格）
- 所有数据独立声明为响应式变量，便于后续接入真实数据
- 提供数据更新方法，支持父组件调用或自动刷新

## MiningTask.vue 右侧内容
- 在 `src/view/MiningTask.vue` 的 `mining-task-right` 区域添加：
  - 模型精度曲线（ECharts 折线图，3条曲线对比）
  - 显卡资源使用率（2个仪表盘：CPU/GPU）
  - 模型详情表格（Element Plus 表格）
- 数据独立声明为响应式变量，支持更新方法

## MiningTask.vue 底部训练列表
- 在 `mining-task-bottom` 区域添加训练列表表格
- 包含列：模型名称、模型、进度、状态（带标签）、创建时间、操作
- 状态标签：成功（绿色）、暂停（黄色）、失败（红色）
- 操作列：7个图标按钮（查看、编辑、复制、播放、文档、详情、删除）
- 数据：`trainingListData`，支持 `updateTrainingListData` 方法更新
